# CRM static site deploy

Render settings:

- Build Command: leave empty
- Publish Directory: `.`

The site entry point is `index.html`. The `_redirects` file keeps `/` routed to `index.html` on Render.
